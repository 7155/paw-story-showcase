{
  "name": "paw-story-showcase-deployment",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "setup": "npm --prefix paw-story-demo ci --include=dev && corepack pnpm --dir control-center-web install --frozen-lockfile --prod=false",
    "build": "npm --prefix paw-story-demo run build",
    "start": "npm --prefix paw-story-demo start",
    "check": "python3 scripts/check_public_showcase.py"
  },
  "packageManager": "pnpm@11.9.0"
}
