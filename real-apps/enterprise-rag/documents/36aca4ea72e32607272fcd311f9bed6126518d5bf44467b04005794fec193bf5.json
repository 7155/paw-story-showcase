{"documentId":"dsid_3d82d20929a949c7abe54d44a383b4b6","source":"linear","title":"Resilient client blueprint: idempotency contract, pragmatic retry defaults, and retryable error mapping","text":"Resilient client blueprint: idempotency contract, pragmatic retry defaults, and retryable error mapping\n\ndescription:\nObjective: produce a cross-language (Python/TypeScript/Go) client blueprint that standardizes retry/backoff defaults, establishes a minimal idempotency contract, and defines a concise retryable-error taxonomy tailored to Redwood Inference workloads. This ticket captures decisions, concrete defaults, ergonomic examples, and an implementation plan for SDK libraries and reference snippets. Goals: (1) minimize accidental duplicate work and unclear semantics for idempotency across SDKs, (2) provide safe out-of-the-box retry defaults for common request types (sync calls, short and long streaming sessions), (3) map known transport, HTTP, and runtime errors to deterministic retry behaviors, and (4) surface telemetry hooks for tuning and rollout.\n\nacceptance_criteria:\nCross-SDK design doc with agreed-on retry presets (fast, conservative, streaming) and exact numeric parameters for backoff/jitter\nIdempotency contract that specifies key generation modes, server semantics expected (at-least-once vs dedupe window), and migration guidance for existing clients\nRetryable-error taxonomy mapping to HTTP status codes, gRPC codes, and transport/SDK-level errors with recommended action (retry, no-retry, safe-fallback)\nReference implementations / code snippets in Python, TypeScript, and Go that demonstrate: client-level retry wrapper, idempotency-key header usage, resumable-stream checkpoint example\nTelemetry plan: SDK emits standardized retry events and idemp key hashing metric; examples wired to Console trace attributes\nIntegration checklist for SDK maintainers and a short migration guide for user apps\n\nimplementation_notes:\nHigh-level approach: (A) Define three named retry presets: 'fast' (low-latency interactive), 'balanced' (default for most requests), 'durable' (for high success at cost of latency). (B) Use exponential backoff with full jitter for all presets; specify base, cap, and max attempts numerically. (C) Idempotency: SDK exposes two modes: 'opaque' (user-provided UUID v4 or client-provided string) and 'derived' (deterministic HMAC of request fingerprint). Library default: no implicit derived idemp for mutable operations unless user opt-in. (D) Streaming: recommend chunk-level checkpoints (sequence numbers + opaque token) paired with windowed dedupe on resume; resumable streams default to 'best-effort' resume only on terminal transport errors and require explicit enablement. (E) Telemetry: emit a single structured event per retry attempt with fields: attempt_number, preset, backoff_ms, error_class, idemp_key_hash. Backward-compatible shim: environment variable SDK_RETRY_PRESET to override presets for quick debugging.\n\ndefaults_table:\nfast: attempts=3, base_delay=50ms, cap_delay=800ms, max_total_time=2s, jitter=full; balanced: attempts=5, base_delay=100ms, cap_delay=1600ms, max_total_time=8s, jitter=full; durable: attempts=8, base_delay=200ms, cap_delay=3200ms, max_total_time=30s, jitter=full\n\nidempotency_contract:\nPurpose: prevent accidental duplicate side-effects while keeping ergonomics reasonable. Server expectation: if an idempotency-key header is supplied, server must dedupe requests with identical key within a configurable window (default 5 minutes) and return a canonical response or a 409 with an 'existing-operation-id' reference. Client responsibilities: when calling mutating endpoints, either supply a stable client-generated key (UUID v4) or enable 'derived' mode where SDK computes key from a canonical serialization + optional customer salt. Keys must be opaque to the server; SDK should also support prefixed keys for multi-tenant debugging. Migration: existing apps using retries without keys should be guided to opt-in by setting idempotency: \"enabled\" in client config and adding a grace period in release notes.\n\nretryable_error_taxonomy:\nRetryable (safe to retry): 502, 503, 504, 429 (with backoff), gRPC Unavailable, DeadlineExceeded (with caution), connection reset / network unreachable, transient TLS handshake failures. Non-retryable: 400, 401, 403 (use immediate fail and surface helpful error), 404 (unless endpoint supports eventual availability semantics), gRPC InvalidArgument, PermissionDenied, Unauthenticated. Conditional-retry: 409 (if operation is known to be idempotent with key), 500 (server-error) should be retried only under 'durable' preset and with server-provided retry-after guidance. Streaming-specific: stream EOF due to network interruption -> eligible for resume; application-level stream error with semantic failure -> no automatic resume. Edge case: partial-response token corruption -> no retry without server-side resume support.\n\nstreaming_ergonomics_and_examples:\nResumable pattern: each server response includes 'resume_token'. Client stream wrapper attaches resume_token on reconnect; SDK exposes on_stream_checkpoint(callback) to persist token to local store.\nChunk ack window: server includes sequence numbers; client should persist last-acked seq and on resume request 'start_after_seq=N'. Dedup window is server-enforced; SDK retries must respect it.\nExample (Python): from redwood import Client\nclient = Client(retry_preset='balanced', idempotency={'mode':'opaque'})\nresponse = client.generate(prompt, idempotency_key=str(uuid.uuid4()))\nExample (TypeScript): const c = new RedwoodClient({ retryPreset: 'fast' }); await c.createCompletion({ prompt, idempotencyKey: uid });\nExample (Go): client := redwood.NewClient(redwood.Options{RetryPreset: redwood.RetryBalanced, Idempotency: redwood.IdempOpaque(uid)})\n\ncode_snippets:\n// Python: simple retry wrapper example\ndef with_retry(fn, preset):\n    attempt = 0\n    while attempt < preset['attempts']:\n        try:\n            return fn()\n        except TransientError as e:\n            attempt += 1\n            backoff = jittered_backoff(preset['base_delay'], attempt, preset['cap_delay'])\n            sleep(backoff)\n    raise\n\n// TypeScript: attaching idempotency header\nconst resp = await fetch('/v1/mutate', {\n  method: 'POST',\n  headers: { 'Idempotency-Key': idempotencyKey },\n  body: JSON.stringify(payload)\n});\n// Go: resumable stream pseudo\nstream, err := client.OpenStream(ctx, req)\nif err != nil { return err }\nfor {\n  msg, err := stream.Recv()\n  if err == io.EOF { break }\n  if err != nil { saveCheckpoint(token); return retryableHandle(err) }\n  handle(msg)\n  saveCheckpoint(msg.ResumeToken)\n}\n\ntelemetry_and_observability:\nEmit structured events at these hooks: request.start, retry.attempt, retry.giveup, idempotency.key_used (hashed), stream.checkpoint. Map these events to Console attributes: route, model_version, retry_preset, attempt_count, error_class. Goal: enable ops to run cohort analysis (e.g., increased retries by model_version) and correlate cost impact.\n\nopen_questions:\n1) Should 'derived' idempotency be opt-in or opt-out for legacy customers? Recommendation: opt-in with explicit compatibility flag. 2) Exact server dedupe window: default 5 minutes recommended, but need product/infra alignment for high-volume tenants. 3) How to surface server-provided retry-after headers for streaming reconnect backoff? Implement a short-circuit in SDK when present. 4) gRPC streaming resume semantics: requires server-side support; document compatibility matrix per model endpoint.\n\ndiscussion:\nSummary of architecture review: engineering leads across Python/TS/Go reviewed numeric defaults and agreed on full jitter + conservative caps to reduce thundering herd. UX trade-off: more retries increase tail latency for interactive paths — hence 'fast' preset exists. Security flagged derived idempotency: ensure HMAC keys are client-side only and never sent to server. PM requested user-visible configuration knobs and migration communication. Design notes include a short rollout plan: ship 'balanced' as default in minor release with opt-in environment variable to flip presets for debugging; follow with telemetry gating and a 4-week observation window before enabling derived idemp patterns in any SDK. Action items: create three PRs (one per language) with adapter and tests, implement telemetry event name schema, and add docs and migration guide on docs.redwood.ai.\n\nnext_steps:\n1) Socialize doc with SDK owners (assign language leads), 2) Implement Python reference PR and tests, 3) Add telemetry schema to Console ingestion team, 4) Draft migration guide and changelog entry, 5) Coordinate server-side dedupe window config with infra team\n\nnotes_history:\n2026-03-10: Draft created by Asha Menon.\n2026-03-12: Design sync with SDK leads (Liam Chen, Priya Kapoor, Mateo Silva) — consensus on presets.\n2026-03-14: Updated defaults and added streaming resume examples after infra feedback.","normalization":{"nulReplacements":0}}